<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skripsi.cuanku">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".reminder.NewReminderActivity"></activity>
        <activity android:name=".reminder.ReminderActivity" />
        <activity android:name=".Add" />
        <activity android:name=".tabungan.DetailTargetActivity" />
        <activity android:name=".tabungan.LihatSemuaTargetActivity" />
        <activity android:name=".tabungan.TambahTabunganActivity" />
        <activity android:name=".tabungan.TambahTargetActivity" />
        <activity android:name=".akun.EditProfilActivity" />
        <activity android:name=".catatan.LihatDataCatatanActivity" />
        <activity android:name=".catatan.TambahCatatanActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".akun.RegisterActivity" />
        <activity android:name=".akun.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".reminder.AlarmReceiver"></receiver>
    </application>

</manifest>